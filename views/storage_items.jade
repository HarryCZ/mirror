extends layoutblock scripts	script.		units = "#{units}";	script(type='text/javascript' src='/js/storageAdd.js')block subnav	.subnav		ul			li <a href="/storage_items"><mtl>storage_items</mtl></a>			li <a href="/units"><mtl>units</mtl></a>block content	h1		mtl=title	table.itemstable		thead			tr				th					mtl.						title				th					mtl.						package				th					mtl.						package_unit				th					mtl.						ration				th					mtl.						ration_unit				th					mtl.						sell_price				th					Deleted		tbody			each item, i in storage_items				tr					td.						#{item.title}					td.						#{item.package}					td.						#{item.package_unit}					td.						#{item.ration}					td.						#{item.ration_unit}					td.						#{item.sell_price}					td.						#{item.deleted}							p.		<br>		<a href="/storage">Zpět</a>block sidebar	.block		h1 			mtl.				add_storage_item		form.addItem(name="additem",method="post",action="/addstorageitem")			input(type="text", placeholder="title", name="title" id="title")			input(type="text", placeholder="package", name="pack")			select(type="text", placeholder="package_unit", name="package_unit", id="package_unit")				each unit, i in uts					option(value="#{unit._id}") #{unit.title}			input(type="text", placeholder="ration", name="ration")			select(type="text", placeholder="ration_unit", name="ration_unit", id="ration_unit")			input(type="text", placeholder="sell_price", name="sell_price")			button#btnSubmit(type="submit") submit