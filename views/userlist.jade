extends layoutblock content	h1		mtl=title	table.usertable		thead			tr				th					mtl.						first_name				th					mtl.						last_name				th					mtl.						username				th					mtl.						email				th					mtl.						language				th					mtl.						password		tbody			each user, i in userlist				tr					td.						#{user.first_name}					td.						#{user.last_name}					td.						#{user.username}					td.						#{user.email}					td.						#{user.language}					td.						#{user.password}	p.		<br>		<a href="http://localhost:3000">Go Back</a>		block sidebar	h1		mtl.			add_user	form.addUser(name="adduser", method="post", action="/adduser")					input(type="text", placeholder="first_name", name="first_name")		input(type="text", placeholder="last_name", name="last_name")		input(type="text", placeholder="username", name="username")		input(type="text", placeholder="email", name="email")		input(type="text", placeholder="language", name="language")		input(type="text", placeholder="password", name="password")		button#btnSubmit(type="submit") add